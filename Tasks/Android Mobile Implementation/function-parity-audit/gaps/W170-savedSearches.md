# W170-W173: Saved Searches

## What the web feature does
Save/load/delete search queries as reusable chips in the search view.

## What exists on Android
Nothing. Search screen has no saved searches feature.

## Fix needed
Add saved search persistence (SharedPreferences or Room) and render as chips above the search results.
