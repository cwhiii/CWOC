# Settings

Settings are organized into groups. Labels and controls are placed inline where space allows. Sub-sections within a group use small headers with a subtle bottom border.

- **General** — Username, time format (24h, 12h, [HST](https://www.cwholemaniii.com/pages/hss/hst.shtml)), sex toggle, snooze length, calendar snapping
- **Period Options** — Week start day, view hours range, scroll-to hour, then enabled periods with per-period options (X Days count, Work Days and Work Hours) nested directly below their checkboxes
- **Tag Editor** — Tree view, create/edit/delete/favorite, "+" for child tags
- **Saved Locations** — Add, label, and set a default location for use across the editor and dashboard weather
- **Chit Options** — Fade past, highlight overdue, delete past alarms, show tab counts, prefer Google for Maps
- **Map Settings** — Auto-zoom to markers toggle, default center latitude/longitude, default zoom level (used by the [Maps View](/maps))
- **Custom Colors** — Two sections: Default Colors (7 palette colors plus Overdue Border and Blocked Border swatches) and Custom Colors (user-added with delete buttons). Click any swatch to assign it as the overdue or blocked border color. Ring indicators show which color is assigned to each role. The overdue border highlights chits past their due date; the blocked border highlights chits with Blocked status.
- **Clocks** — Choose active clocks, drag to reorder, set orientation (horizontal/vertical)
- **Custom Filters & Sorting** — Per-view custom filter and sort defaults. Click a view button to open a modal with the full sidebar filter UI (status, priority, display toggles, text search, sort). Save to auto-apply those filters every time you switch to that view. Reset to Defaults reverts to system defaults.
- **Visual Indicators** — Per-indicator visibility (Always / Never / If Space), combine alerts toggle
- **Habits** — Success rate window (7/30/90 days or all time), default show habits on calendar toggle
- **Default View** — Choose which dashboard view loads when you first open the site (Calendar, Checklists, Tasks, Projects, Notes, Email, Indicators, Alerts, or Omni). Found under the Views tab in [Settings → Default View](/frontend/html/settings.html#default-view)
- **Version & Updates** — Current version, upgrade button, update log, restart service (admin only)
- **Data** — Audit log link, trash link, [audit log](/frontend/html/audit-log.html) pruning limits
- **[Data Management](/frontend/html/help.html#data-management)** — JSON export/import for chit data and user data (add or replace modes), plus iCalendar (.ics) import for Google Calendar, Google Tasks import, and Google Keep import
- **Kiosk** — Select which tags to display on the unauthenticated kiosk view, and launch it
- **Dependent Apps** — (Admin only) Configure external services: Tailscale VPN for remote access, Ntfy for push notifications

---

**See also:** [Saved Locations](/frontend/html/help.html#saved-locations) · [Visual Indicators](/frontend/html/help.html#visual-indicators) · [Clocks](/frontend/html/help.html#clocks) · [Data Management](/frontend/html/help.html#data-management) · [Version & Updates](/frontend/html/help.html#version-management)
