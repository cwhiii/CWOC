# Tests Are Optional in Specs

## Rules

- Writing and executing tests are OPTIONAL tasks in specs.
- Do not treat test tasks as blocking or required for spec completion.
- Do not auto-generate test tasks unless the user explicitly requests them.
- If a spec includes test tasks, they may be skipped without affecting the validity of the implementation.
