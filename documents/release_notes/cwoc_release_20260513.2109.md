# Release 20260513.2109

Fixed the "Loading chits" spinner appearing when returning from the editor. The cache-first render now runs immediately on page load (before waiting for settings), so the dashboard appears instantly from cached data.
