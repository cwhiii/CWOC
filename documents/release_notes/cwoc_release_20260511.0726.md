Added disk storage display to Version & Updates section showing both system disk usage (used/total with percentage) and CWOC-specific data storage (DB, attachments, contacts, user images). Updates on page load and via refresh button.

Added configurable session lifetime setting in Administration. Options: 1 hour, 12 hours, 24 hours, 1 week, 1 month, or never. Applies on next login. Both absolute expiry and inactivity timeout respect the configured value.
