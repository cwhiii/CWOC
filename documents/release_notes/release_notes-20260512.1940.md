Custom Objects editor: fixed page icon (was showing "fas fa-cubes" as text, now uses 🧊 emoji). Swapped unit field order — metric is now the primary/default, imperial labeled as such. Renamed range fields to "Ideal Range Min/Max". Improved Type/Sub-Type field hints to clarify you can type new values. Improved Quick Log tooltip. Removed category field entirely — consolidated to type + sub_type with migration. Fixed syntax error in editor-health.js.
