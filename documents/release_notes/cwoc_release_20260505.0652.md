## Release 20260505.0652

Global search tag logic now defaults to AND — `#work #urgent` requires both tags. Use `||` between tags for OR (`#work || #personal`). Added an info hint below the search bar explaining the syntax.
