Overhauled notification lifecycle. Notifications persist forever — dismiss changes status (not delete). Notifications view has Unread/Addressed sections, both sorted most-recent-first. Accept/Decline pill toggle always visible on sharing notifications in both sections (with active state highlighting). Each card has a 🗑️ delete button. "Clear Addressed" bulk-deletes. Desktop reminders auto-dismiss after first toast. Backend accepts "dismissed" status.
