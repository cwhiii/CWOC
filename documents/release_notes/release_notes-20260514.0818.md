Tag filter bar: added shared "Any Tag" / "Tagless" virtual options and Shift+Click to select only one tag. Extracted all tag filter logic (UI rendering, state management, chit matching) into shared functions (`cwocLoadTagFilter`, `cwocChitPassesTagFilter`, `cwocClearTagFilter`, `_cwocUpdateTagVirtualOptions`) in `shared-sidebar-filter.js`. Both dashboard and maps page now call the same shared code — no duplication.
