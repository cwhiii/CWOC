Overhauled notification lifecycle. Notifications persist forever with Unread/Addressed sections. Accept/Decline pill toggle always visible on sharing notifications (fixed missing data-val attributes for proper CSS styling). Desktop reminders labeled "Next Time On Desktop" instead of "desktop-only". Each card has 🗑️ delete. "Clear Addressed" bulk-deletes. Desktop reminders auto-dismiss after first toast.
