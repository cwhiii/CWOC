Desktop-only notifications feature with full notification actions. Editor alerts "next time on desktop" option defers delivery until non-mobile session. All notification surfaces (sidebar inbox, profile menu, Alarms→Notifications view) now show Snooze + Dismiss for reminder-type notifications. Snooze uses the user's default snooze length from settings. Backend: new POST /api/notifications/{id}/snooze endpoint, snoozed_until column on notifications table, GET filters out snoozed notifications automatically. Triggered desktop notifications show crossed-out with ✅ in the editor.
