Removed the `category` field from Custom Objects — consolidated to just `type` (top-level grouping) and `sub_type` (subdivision). Migration copies existing category values into sub_type and updates the unique index. Frontend editor, indicators zone, and graph picker all updated to use sub_type for section grouping. Add Indicator picker now fetches all objects from the registry. Indicators zone renders in a 3-column grid grouped by collapsible sub_type sections.
