# Release 20260514.0734

Changed tag creation on the settings page: Enter now quick-creates the tag immediately with default color (no modal). Shift+Enter opens the full tag modal for color, sharing, and other options. Also includes the 401 retry fix, partial-update tag operations, and custom_view_filters migration from the previous build.
