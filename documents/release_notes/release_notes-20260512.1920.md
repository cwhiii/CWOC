Health Indicators zone: grouped fields by category in collapsible sections with 3-column grid (1 on mobile). Fixed zone_config mismatch. "Add Indicator" picker now fetches ALL custom objects from the registry (not just zone-assigned ones), so Symptoms and other unassigned objects appear. Picker and per-chit additions grouped by category. Dashboard "Add Graph" replaced with collapsible sidebar section by category.
