## Release 20260505.0631

Global search now supports mixed `#tag` and text queries. You can search `#work meeting` to find chits tagged "work" that also mention "meeting", or use `#tag1 #tag2` to require multiple tags, or just `#tag` alone to filter by tag only.
